# .github/workflows/build-glenda-windows.yml
name: Build Glenda Desktop Windows

on:
  workflow_dispatch:
  push:
    branches: [release-v1.12.15-dev]

jobs:
  prepare:
    uses: ./.github/workflows/build-glenda-prepare.yml

  build-windows:
    needs: prepare
    uses: ./.github/workflows/build_desktop_windows.yaml
    with:
      arch: x64
      sign: ''
      test: false
      targets: msi